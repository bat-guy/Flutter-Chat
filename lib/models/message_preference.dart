enum MessageColorPreference {
  senderBackgroundColor,
  senderTextColor,
  receiverBackgroundColor,
  receiverTextColor,
  senderTimeColor,
  receiverTimeColor,
  dateBackgroundColor,
  dateTextColor,
}

enum MessageSizePreference {
  messageTextSize,
  messageTimeSize,
  dateTextSize,
}
